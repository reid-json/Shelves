version: 0.2
phases:
  install:
    commands:
      - cd frontend/app
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync dist/ s3://my-vue-app-bucket-shelves --delete
artifacts:
  files:
    - '**/*'